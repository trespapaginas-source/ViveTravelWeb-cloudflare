## Plan: Limpieza de paleta + unificación de iconografía

### Decisiones confirmadas
- **Primary:** mantener `ocean` (#008B8B) — NO migrar a #0D5C56 (evita repintar todo el sitio).
- **Footer:** cambiar fondo a `ocean.dark` (#005C5C), tokenizando el hex suelto.
- `lucide-react` YA está instalado y en uso en 32 archivos — no se instala nada.

### Hallazgos clave del audit (corrigen la premisa del brief)
- **NO existen** `bg-blue-*`, `text-cyan-*`, `bg-cyan-*` en el código (ya migrados a teal).
- **NO existe** fondo azul en el footer (es `#090E1A`, navy casi-negro).
- El único color off-brand real es **`emerald-*`**: 12 usos en 7 archivos.
- Los únicos SVG inline reemplazables son los 3 iconos genéricos de `promotions-banner.tsx`.

---

### Cambio 1 — Eliminar `emerald-*` (12 usos → reemplazo por tokens de marca/semánticos)

| Archivo:línea | Actual | Nuevo |
|---|---|---|
| `footer.tsx:185` | `group-hover:text-emerald-400` (Phone) | `group-hover:text-ocean-light` |
| `footer.tsx:194` | `group-hover:text-emerald-400` (Mail) | `group-hover:text-ocean-light` |
| `footer.tsx:217` | `text-emerald-400` (NIT) | `text-ocean-light` |
| `footer.tsx:219` | `text-emerald-400` (RNT) | `text-ocean-light` |
| `plan-detail.tsx:207` | `text-emerald-500` (Check "incluye") | `text-ocean` |
| `cabin-detail.tsx:180` | `text-emerald-500` (Check amenidades) | `text-ocean` |
| `transport-detail.tsx:98` | `text-emerald-500` (Check) | `text-ocean` |
| `transports.tsx:66` | `text-emerald-500` (Check feature) | `text-ocean` |
| `visa-detail.tsx:110` | `text-emerald-500` (CheckCircle2) | `text-ocean` |
| `contact.tsx:112` | `text-emerald-500` (CheckCircle2 éxito) | `text-ocean` |
| `visas.tsx:15` | `bg-emerald-100 text-emerald-700` (badge "free") | `bg-ocean/10 text-ocean` |
| `visa-detail.tsx:22` | `bg-emerald-100 text-emerald-700` (badge "free") | `bg-ocean/10 text-ocean` |

### Cambio 2 — Footer: fondido y cleanup
- `footer.tsx:64`: `bg-[#090E1A]` → `bg-ocean-dark` (tokeniza el hex + fondo más cálido de marca).
- El resto del footer (`text-slate-*`, `border-slate-*`) se mantiene — son neutrales coherentes con el fondo oscuro.

### Cambio 3 — Iconografía: 3 SVG inline → lucide en promotions-banner
- `promotions-banner.tsx:127-135`: reemplazar la función `ValueIcon` (3 `<svg>` inline) por iconos lucide equivalentes: `Package` (promos), `CreditCard` (medios de pago), `Headset`/`User` (agente). Se importan de `lucide-react` y se renderizan con `className="h-5 w-5"`.
- **NO se tocan** los brand marks inline (TikTok en footer, Google "G" en testimonials, WhatsApp en whatsapp-fab y plan-detail) — no tienen equivalente fiel en lucide y son logos de marca.

### Cambio 4 — (sin acción) lo que YA cumple el brief
- `lucide-react` ya es la librería única (0 react-icons/heroicons/emoji).
- Sin `bg-blue-*` ni `text-cyan-*` que eliminar.
- Paleta `ocean` centralizada en tailwind.config.js + index.css (CSS vars `--primary`/`--secondary`/`--ring`).

---

### Verificación
- `npx tsc --noEmit` sin errores.
- `npm run build` exitoso.
- `git push` para deploy a Cloudflare.
- Grep final para confirmar 0 ocurrencias de `emerald-` en `src/`.
