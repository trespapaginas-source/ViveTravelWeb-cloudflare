## Plan: Migración a paleta 95% neutral / 5% acento

### Decisiones confirmadas
- **Acento dual (se conserva):** Botón circular del Hero = `ocean` (#008B8B); FAB flotante = WhatsApp green (#1DA851). Ambos son los únicos puntos de color de la app.
- **Footer:** imagen de fondo + overlay oscuro (spec). No existe `/images/footer-bg.jpg` → reuso `sky-banner-bg.jpg` (asset existente, temática coherente) con overlay `bg-neutral-950/85`.
- **Escopo:** migrar ~210 instancias de `ocean` en 33 archivos a neutrales (black/grayscale), conservando SOLO el botón del Hero.

### Mapeo de reemplazos (regla global por bucket)

| Token actual | Reemplazo | Rationale |
|---|---|---|
| `text-ocean` / `text-ocean-light` (íconos, títulos, links) | `text-neutral-900` (énfasis) / `text-neutral-600` (muted) | Iconografía y texto 100% monocromo |
| `bg-ocean` (tabs activos, badges, pills) | `bg-neutral-900 text-white` (activo) | Tabs/pills en negro neutral |
| `bg-ocean/10` (estados seleccionados) | `bg-neutral-100 text-neutral-900 font-semibold border border-neutral-300` | Selección neutral |
| `bg-ocean` / `bg-ocean-dark` (fondos grandes) | `bg-neutral-950` (footer) / `bg-neutral-50` (secciones claras) | Fondos neutrales |
| `focus:border-ocean` / `focus-within:border-ocean` | `focus:border-neutral-900` | Foco neutral |
| `accent-ocean` (sliders) | `accent-neutral-900` | Track del slider neutral |
| `border-ocean` / `ring-ocean` | `border-neutral-300` / `ring-neutral-900` | Bordes neutrales |

### Cambios de alto impacto primero (1 cambio arregla mucho)

1. **`src/index.css:85`** — `focus:border-ocean` → `focus:border-neutral-900`. Arregla los focos de TODOS los inputs de la app de golpe.
2. **`src/App.tsx:49`** — spinner global `border-ocean` → `border-neutral-900`. Loader de rutas neutral.
3. **`tailwind.config.js`** — comentar/eliminar los aliases `coral` y `indigo` (ambos = #008B8B, hoy sin uso tras la migración).

### Hero — botón circular (spec punto 3)

`hero-section.tsx:451-463`: convertir el botón "Buscar" rectangular en **circular, solo icono**:
- Clases: `w-12 h-12 rounded-full bg-ocean text-white flex items-center justify-center hover:scale-105 transition-all shadow-md md:h-full md:w-12`
- Quitar texto "Buscar"/"Ir", dejar solo `<Search className="w-5 h-5 text-white" />`
- Quitar `COL[cols.buscar]` (span variable) → fijo `md:col-span-1`
- Conserva `bg-ocean` ✅ (única excepción)
- Otros ocean del Hero (líneas 248, 554, 582, 754) → neutral

### Catalog (`plans.tsx`) — puntos 4A/B/C del spec

- **Tabs categoría (112):** activo `bg-ocean` → `bg-neutral-900`; inactivo queda `bg-muted` → `bg-neutral-100 text-neutral-600 hover:bg-neutral-200`
- **Slider precio (376):** `accent-ocean` → `accent-neutral-900`
- **Duración seleccionada (399):** `bg-ocean/10 text-ocean` → `bg-neutral-100 text-neutral-900 font-semibold border border-neutral-300`
- **Paginación activa (195):** `bg-ocean` → `bg-neutral-900`
- **CTA móvil resultados (241):** `bg-ocean` → `bg-neutral-900`
- **Filtros focus (294,310,346):** → `focus:border-neutral-900`

### Badges de tarjetas (punto 4C)

- `plan-card.tsx:32` "★ Destacado": `bg-ocean` → `bg-black/60 backdrop-blur-md text-white border border-white/10` (glassmorphism del spec)
- `cabin-card.tsx:27,109`: mismo tratamiento

### Footer (punto 5 del spec)

`footer.tsx:64`: reescribir wrapper:
```tsx
<footer className="relative bg-cover bg-center text-white"
  style={{ backgroundImage: "url('/images/sky-banner-bg.jpg')" }}>
  <div className="absolute inset-0 bg-neutral-950/85 backdrop-blur-[2px]" />
  <div className="relative z-10 max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8 lg:py-10">
    {/* contenido existente */}
  </div>
</footer>
```
- `text-ocean-light` (NIT/RNT + hover iconos) → `text-white` / `group-hover:text-white`

### Navbar — estados activos móviles

- Líneas 298-299, 340, 359-360, 376-377: `bg-ocean/10 text-ocean` → `bg-neutral-100 text-neutral-900 font-semibold`; hover `hover:text-ocean` → `hover:text-neutral-900`
- Desktop activo (zinc) y CTA Reservar (zinc): **sin cambios** (ya neutrales)
- Campaign banner (81): `bg-ocean` → `bg-neutral-900`

### Barrido del resto (íconos + texto + fondos, plan-detail/cabin-detail/visa-detail/etc.)

Aplicar el mapeo global a los ~16 íconos (Bucket A) y ~54 textos (Bucket B) detectados. Ejemplos:
- `Check`/`CheckCircle2` con `text-ocean` → `text-neutral-700`
- Títulos/spans `text-ocean` → `text-neutral-900`
- `bg-ocean` gradientes (contact submit, favorites CTA) → `bg-neutral-900`

### Verificación
- `grep -rEn "bg-ocean|text-ocean|border-ocean|accent-ocean|ring-ocean|fill-ocean" src/` → debe retornar SOLO `hero-section.tsx` (el botón circular).
- `npx tsc --noEmit` sin errores.
- `npm run build` exitoso.
- `git push` para deploy.

### Nota de riesgo
~210 ediciones en 33 archivos. Lo hago en orden: primero los de alto impacto (index.css, App.tsx, footer, hero, plans, navbar), luego el barrido de páginas de detalle. Verifico con grep tras cada lote.
